services:
  bcp:
    build: .
    container_name: bcp-calculadora
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-2.5-flash}
